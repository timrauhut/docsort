# Copy values into your shell or a local untracked env file before deploy.
# Do NOT commit real secrets or real LAN IPs.

# Topology (used by DEPLOY-AGENTS recipes / bin/setup-lan-tls)
# Also put the same IP in config/deploy.yml (use skip-worktree so it stays local).
# export DOCSORT_HOST=192.168.0.1
# export DOCSORT_SSH_USER=pi
# export DOCSORT_SSH_PORT=22
# export DOCSORT_SSH_KEY=$HOME/.ssh/id_ed25519
# export DOCSORT_LAN_IP=$DOCSORT_HOST

# Required
export WEBDAV_PASSWORD='generate-a-long-random-password'

# Optional model pull override (bin/kamal-setup / bin/kamal-ollama pull)
export OLLAMA_PULL_MODEL='llama3.2'
# On Raspberry Pi 5 prefer:
# export OLLAMA_PULL_MODEL='llama3.2:1b'

# Registry (when not using localhost:5555)
# export KAMAL_REGISTRY_USERNAME='you'
# export KAMAL_REGISTRY_PASSWORD='token'

# Optional
# export GITHUB_TOKEN='ghp_...'
# Maximum accepted document size in bytes (default: 104857600 / 100 MB)
DOCSORT_MAX_UPLOAD_BYTES=104857600

# Keep false in production. WebDAV uses Basic auth and must be protected by HTTPS.
DOCSORT_ALLOW_INSECURE_AUTH=false
DOCSORT_ALLOW_INSECURE_WEBDAV=false
