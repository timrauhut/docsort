<% content_for :title, "New user · DocSort" %>

<div class="page-hero">
  <div>
    <p class="eyebrow">Admin</p>
    <h1>New user</h1>
    <p class="lede">Create a login that also works for WebDAV uploads.</p>
  </div>
  <%= link_to "← Users", users_path, class: "btn btn-ghost btn-sm" %>
</div>

<section class="card card-pad user-form-card">
  <%= render "form", user: @user %>
</section>
