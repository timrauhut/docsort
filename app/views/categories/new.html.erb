<% content_for :title, "New category · DocSort" %>

<div class="narrow">
  <div class="page-hero">
    <div>
      <p class="eyebrow">Structure</p>
      <h1>New category</h1>
      <p class="lede">Creates a sorting folder under storage/sorted/.</p>
    </div>
  </div>
  <%= render "form", category: @category %>
</div>
