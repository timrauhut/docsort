# Kamal secrets — sourced at deploy time. Safe patterns only (no raw secrets in git).
# Docs: https://kamal-deploy.org/docs/configuration/environment-variables/

# Rails credentials master key (required)
RAILS_MASTER_KEY=$(cat config/master.key)

# Bootstrap admin password (web UI + WebDAV). Prefer DOCSORT_ADMIN_PASSWORD;
# WEBDAV_PASSWORD is still accepted as a legacy alias.
#   export DOCSORT_ADMIN_PASSWORD='a-long-random-string'
DOCSORT_ADMIN_PASSWORD=${DOCSORT_ADMIN_PASSWORD:-${WEBDAV_PASSWORD:?Set DOCSORT_ADMIN_PASSWORD (or WEBDAV_PASSWORD) before deploying}}
